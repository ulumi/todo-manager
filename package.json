{
  "name": "todo-manager-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local API server for todo-manager (dev only)",
  "main": "server.js",
  "scripts": {
    "server": "node server.js",
    "dev": "npx sass --watch css/styles.scss:css/styles.css --style=expanded"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1",
    "firebase-admin": "^12.0.0",
    "interactjs": "^1.10.27"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "sass": "^1.98.0"
  }
}
